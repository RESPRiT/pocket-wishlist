node_modules
.DS_Store
*.un~
.env
.env.local
.env.production

# Local dev artifacts from packages/wishlist-api scripts (probe HTML dumps,
# local SQLite). Never want these tracked.
mall*.html
local.db
local.db-*

# Beads / Dolt files (added by bd init)
.dolt/
*.db
*.db-shm
*.db-wal
.beads-credential-key

# Claude Code local-only state
.claude/worktrees/
.claude/settings.local.json

# Beads passive exports — source of truth is the Dolt DB.
# Tracking these creates churn on every commit via hooks.
.beads/issues.jsonl
.beads/interactions.jsonl
*.swp
*.swo
